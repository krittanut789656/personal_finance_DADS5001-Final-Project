"""
Shared Streamlit application layer (Phase 3).

This package contains code shared across `app.py` and all pages in
`pages/`: backend path bootstrap, cached data/resource accessors,
session_state defaults, and UI helpers (theme, KPI cards, filters).
"""
